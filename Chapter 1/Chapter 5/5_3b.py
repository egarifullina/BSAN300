# Add a new key/value pair to a dictionary using the subscript operator []

info = {}
info["name"] = "Sandy"
info["occupation"] = "hacker"

print(info)

# Use [] to replace a value in an existing key 
info["occupation"] = "manager"

print(info)